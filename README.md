# UnNano
A GUI Tool for Converting Thorlabs Educational AFM Data to 3D Printable Mesh

## Usage

1. **Prepare the directory**:  
   Make sure the project is in a folder with a subfolder named `model_template`.

2. **Install dependencies**:  
   Run the following command to install the required dependencies:  
   ```bash
   pip install -r requirements.txt
